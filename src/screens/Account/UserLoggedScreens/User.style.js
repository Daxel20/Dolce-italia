import { StyleSheet } from "react-native";

export const style=StyleSheet.create({
    image:{
        resizeMode:"contain",
        heigh:200,
        width: "100%",
        marginTop: 20,
        alignItems:"center",
    },
});
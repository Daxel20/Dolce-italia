import React from "react";
import { View,Text } from "react-native";

const FavoriteScreens = () => {
    return(
        <View>
            <Text> Estamos en la screen de Favorite</Text>
        </View>

    );
};
export default FavoriteScreens;